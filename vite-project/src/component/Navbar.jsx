import ReactIcon from "/React-icon.png"

export default function Navbar (){
  return(
    <header>
        <nav>
            <img src= {ReactIcon}/>
            <span>ReactFacts</span>
        </nav>
    </header>
  )
  }
